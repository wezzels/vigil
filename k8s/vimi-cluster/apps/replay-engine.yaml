---
# replay-engine — VIMI Mission App
apiVersion: apps/v1
kind: Deployment
metadata:
  name: replay-engine
  namespace: vimi
  labels:
    app: vimi
    component: replay-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vimi
      component: replay-engine
  template:
    metadata:
      labels:
        app: vimi
        component: replay-engine
    spec:
      containers:
      - name: replay-engine
        image: vimi/replay-engine:latest
        imagePullPolicy: Never
        ports:
        - name: http
          containerPort: engine
          protocol: TCP
        env:
        - name: KAFKA_BROKERS
          value: "kafka:9092"
        - name: REDIS_ADDR
          value: "redis:6379"
        - name: DIS_SITE_ID
          value: "1"
        - name: DIS_APP_ID
          valueFrom:
            configMapKeyRef:
              name: vimi-config
              key: DIS_APP_ID
              optional: true
        - name: PORT
          value: "engine"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 9092
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: 9092
          initialDelaySeconds: 5
          periodSeconds: 10
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: replay-engine
  namespace: vimi
  labels:
    app: vimi
    component: replay-engine
spec:
  type: ClusterIP
  ports:
  - name: http
    port: engine
    targetPort: http
    protocol: TCP
  selector:
    app: vimi
    component: replay-engine
