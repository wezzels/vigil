apiVersion: apps/v1
kind: Deployment
metadata:
  name: sensor-fusion
  namespace: vimi
  labels:
    app: vimi
    component: sensor-fusion
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vimi
      component: sensor-fusion
  template:
    metadata:
      labels:
        app: vimi
        component: sensor-fusion
    spec:
      containers:
      - name: sensor-fusion
        image: darth:5000/vimi-sensor-fusion:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8082
          protocol: TCP
        env:
        - name: KAFKA_BROKERS
          value: kafka:9092
        - name: REDIS_ADDR
          value: redis:6379
        - name: DIS_SITE_ID
          value: '1'
        - name: DIS_APP_ID
          valueFrom:
            configMapKeyRef:
              name: vimi-config
              key: DIS_APP_ID
              optional: true
        - name: PORT
          value: '8082'
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        resources:
          requests:
            memory: 64Mi
            cpu: 50m
          limits:
            memory: 512Mi
            cpu: 500m
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 10
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: sensor-fusion
  namespace: vimi
  labels:
    app: vimi
    component: sensor-fusion
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8082
    targetPort: 8082
    protocol: TCP
  selector:
    app: vimi
    component: sensor-fusion
