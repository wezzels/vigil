apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: vimi
    component: data-catalog
  name: data-catalog
  namespace: vimi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vimi
      component: data-catalog
  template:
    metadata:
      labels:
        app: vimi
        component: data-catalog
    spec:
      containers:
      - env:
        - name: KAFKA_BROKERS
          value: kafka:9092
        - name: REDIS_ADDR
          value: redis:6379
        - name: DIS_SITE_ID
          value: '1'
        - name: DIS_APP_ID
          valueFrom:
            configMapKeyRef:
              key: DIS_APP_ID
              name: vimi-config
              optional: true
        - name: PORT
          value: '8087'
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: 172.17.0.1:5000/vimi-data-catalog:latest
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /health
            port: 8087
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
        name: data-catalog
        ports:
        - containerPort: 8087
          name: http
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /health
            port: 8087
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 50m
            memory: 64Mi
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: vimi
    component: data-catalog
  name: data-catalog
  namespace: vimi
spec:
  ports:
  - name: http
    port: 8087
    protocol: TCP
    targetPort: 8087
  selector:
    app: vimi
    component: data-catalog
  type: ClusterIP
