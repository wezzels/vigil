apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: vimi
    component: alert-dissemination
  name: alert-dissemination
  namespace: vimi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vimi
      component: alert-dissemination
  template:
    metadata:
      labels:
        app: vimi
        component: alert-dissemination
    spec:
      containers:
      - env:
        - name: KAFKA_BROKERS
          value: kafka:9092
        - name: REDIS_ADDR
          value: redis:6379
        - name: DIS_SITE_ID
          value: '1'
        - name: DIS_APP_ID
          valueFrom:
            configMapKeyRef:
              key: DIS_APP_ID
              name: vimi-config
              optional: true
        - name: PORT
          value: '8084'
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: darth:5000/vimi-alert-dissemination:latest
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
        name: alert-dissemination
        ports:
        - containerPort: 8084
          name: http
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 50m
            memory: 64Mi
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: vimi
    component: alert-dissemination
  name: alert-dissemination
  namespace: vimi
spec:
  ports:
  - name: http
    port: 8084
    protocol: TCP
    targetPort: 8084
  selector:
    app: vimi
    component: alert-dissemination
  type: ClusterIP
